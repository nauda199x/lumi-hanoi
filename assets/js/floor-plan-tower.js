(()=>{const app=document.querySelector('[data-floor-plan-app]');if(!app)return;const tower=app.dataset.tower;const phaseName={signature:'Signature',prestige:'Prestige',elite:'Elite'};const esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));fetch('/assets/data/floor-plans.json',{cache:'no-cache'}).then(r=>{if(!r.ok)throw new Error('manifest');return r.json()}).then(data=>{const item=data.towers&&data.towers[tower];if(!item)throw new Error('tower');const phase=phaseName[item.phase]||item.phase;const nav=item.plans.map(p=>`<a href="#${esc(p.anchor)}">${esc(p.label)}</a>`).join('');const plans=item.plans.map(p=>{const src=p.asset||`https://drive.google.com/thumbnail?id=${encodeURIComponent(p.driveId)}&sz=w2400`;const original=p.driveId?`https://drive.google.com/file/d/${encodeURIComponent(p.driveId)}/view`:src;return `<section class="floor-plan-section" id="${esc(p.anchor)}" data-tower="${esc(tower)}"><div class="plan-section-head"><div><p class="eyebrow">${esc(phase)} · ${esc(tower)}</p><h2>${esc(tower)} — ${esc(p.label)}</h2></div><a class="source-mini" href="${esc(original)}" rel="noopener noreferrer">Xem mặt bằng gốc ↗</a></div><figure class="figure floor-plan-figure"><a href="${esc(src)}" data-lightbox data-lightbox-alt="Mặt bằng tòa ${esc(tower)} Lumi Hanoi — ${esc(p.label)}" data-lightbox-caption="Tòa ${esc(tower)} — ${esc(p.label)}"><img class="figure-image floor-plan-image" src="${esc(src)}" width="2400" height="3200" alt="Mặt bằng tòa ${esc(tower)} Lumi Hanoi — ${esc(p.label)}" loading="lazy" decoding="async"></a><figcaption class="figure-caption">Mặt bằng ${esc(tower)} — ${esc(p.label)}.<span class="figure-source">Nguồn tệp: ${esc(p.source)}${p.driveId?` · Drive ID ${esc(p.driveId)}`:''}</span></figcaption></figure></section>`}).join('');let supplemental='';if(Array.isArray(data.supplemental)){supplemental=data.supplemental.filter(x=>x.tower===tower).map(x=>`<section class="floor-plan-section supplemental-plan" id="${esc(tower.toLowerCase())}-supplemental-${esc(x.floorLabel).replace(/\s+/g,'-')}"><div class="plan-section-head"><div><p class="eyebrow">${esc(phase)} · ${esc(tower)}</p><h2>${esc(tower)} — ${esc(x.floorLabel)}</h2></div><a class="source-mini" href="${esc(x.sourceUrl)}" rel="noopener noreferrer">Mở tài liệu gốc ↗</a></div><div class="pdf-source-card"><strong>${esc(x.sourceFileName)}</strong><p>Tài liệu nguồn riêng cho ${esc(x.floorLabel)}. Giữ nguyên dưới dạng tài liệu gốc để tránh gộp nhầm với nhóm tầng khác.</p><a class="btn" href="${esc(x.sourceUrl)}" rel="noopener noreferrer">Xem ${esc(x.floorLabel)}</a></div></section>`).join('')}app.innerHTML=`<nav class="floor-plan-index tower-floor-index sticky-floor-index" aria-label="Chọn nhóm tầng tòa ${esc(tower)}"><strong>Chọn tầng</strong>${nav}</nav>${plans}${supplemental}`;if(window.LumiLightbox&&typeof window.LumiLightbox.refresh==='function')window.LumiLightbox.refresh()}).catch(()=>{app.innerHTML='<p class="notice">Không tải được thư viện mặt bằng lúc này. Vui lòng quay lại trang tổng hợp mặt bằng.</p>'})})();